services:
  app:
    build: .
    container_name: shortener-app
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
      DB_HOST: /app/data
      DB_NAME: database.sqlite
    volumes:
      - ./data:/app/data
    restart: unless-stopped